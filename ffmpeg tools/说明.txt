1、在https://github.com/BtbN/FFmpeg-Builds/tags下载ffmpeg，然后添加环境变量。
2、这里把ffmpeg放在D盘，然后此电脑右键属性->高级系统设置->最下面环境变量->用户变量->把ffmpeg下的bin目录添加到PATH变量名里，这里我的是D:\ffmpeg\bin
3、运行对应的代码或程序。（输出文件名一定要加上后缀，如果文件名不带路径，会在py文件所在目录下生成文件；否则在指定的路径下面生成文件。）